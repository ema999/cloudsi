doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width,initial-scale=1,shrink-to-fit=no')
    meta(name='theme-color', content='#000000')
    link(rel='manifest', href='/manifest.json')
    link(rel='shortcut icon', href='/favicon.ico')
    title React App
    link(href='/static/css/main.7ede8f93.css', rel='stylesheet')
  body
    noscript You need to enable JavaScript to run this app.
    script.
      let jsonFromJade = "#{inicialState}".replace(/&quot;/g, '"');
      if (jsonFromJade != '#{inicialState}') {
      const inicialState = JSON.parse(jsonFromJade);
      window.inicialState = inicialState;
      }
    #root
    script(type='text/javascript', src='/static/js/main.d66a3db7.js')
